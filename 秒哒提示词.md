
## 项目概述

一个在线的 MIDI 转《我的世界》钢琴模组乐谱工具。玩家上传 MIDI 文件，转换成游戏内可导入的乐谱 JSON 数据，支持复制到剪贴板、下载 JSON 文件、以及从 JSON 反向预览音符信息。兼容 PC 和手机端。

## 功能需求

### 1. MIDI 文件上传
- 支持拖拽上传和点击选择文件
- 支持 .mid 和 .midi 格式
- 上传后显示文件名和文件大小

### 2. MIDI → 乐谱 JSON 转换（核心功能）
使用 JavaScript 解析 MIDI 文件（可用 midi-parser-js 或 tonejs/Midi 等库），按以下规则转换：

**音符范围过滤：**
- 只保留 MIDI 音符编号 60~95（对应 C4~B6）范围内的音符
- 只保留白键（C、D、E、F、G、A、B），跳过黑键（半音）

**MIDI 音符到琴键索引映射表：**
```
MIDI 60(C4) → 14     MIDI 72(C5) → 7      MIDI 84(C6) → 0
MIDI 62(D4) → 15     MIDI 74(D5) → 8      MIDI 86(D6) → 1
MIDI 64(E4) → 16     MIDI 76(E5) → 9      MIDI 88(E6) → 2
MIDI 65(F4) → 17     MIDI 77(F5) → 10     MIDI 89(F6) → 3
MIDI 67(G4) → 18     MIDI 79(G5) → 11     MIDI 91(G6) → 4
MIDI 69(A4) → 19     MIDI 81(A5) → 12     MIDI 93(A6) → 5
MIDI 71(B4) → 20     MIDI 83(B5) → 13     MIDI 95(B6) → 6
```

**时间处理：**
- 读取 MIDI 文件的 BPM（每分钟节拍数，默认 120）
- 输出格式以 tick 为单位，1 tick = 1/4 拍
- 从 MIDI ticks 转换为输出 ticks：adjusted_start = midi_start * 4 / midi_ticks_per_beat
- 持续时间同样转换，最小为 1 tick
- 将时间对齐到 tick 0 开始

**音量处理：**
- MIDI velocity 0-127 映射到音量 0.3-1.0
- 公式：volume = max(0.3, min(1.0, velocity / 127))

**输出 JSON 格式：**
```json
{
    "v": 1,
    "n": "曲名（从文件名或MIDI元数据获取）",
    "b": 120,
    "t": [
        [keyIndex, startTick, duration, volume],
        [7, 0, 1, 1.0],
        [9, 0, 1, 1.0],
        [14, 4, 4, 0.8]
    ]
}
```

### 3. 转换信息展示
转换完成后显示：
- 曲名、BPM
- 有效音符数量、总时长（秒）
- 跳过的黑键数和范围外音符数
- 音符详情列表（前 20 个），格式："C5(idx=7) @tick=0 dur=1 vol=1.0"

### 4. 结果预览与操作
- 显示格式化的 JSON 预览（带语法高亮）
- 三个操作按钮：
  - 「复制 JSON」- 复制紧凑格式（无空格缩进）到剪贴板
  - 「下载 JSON」- 保存为 .json 文件
  - 「复制到剪贴板（紧凑）」- 复制一行紧凑 JSON 用于游戏导入

### 5. 导入乐谱功能
- 一个文本输入框，玩家可以粘贴 JSON 数据
- 点击「解析预览」按钮验证 JSON 格式并显示曲名、音符数等信息
- 如果格式正确，显示绿色成功提示；如果错误，显示具体错误信息

### 6. 使用说明区域
折叠面板展示游戏内导入步骤：
1. 转换 MIDI 或粘贴乐谱 JSON
2. 点击「复制到剪贴板」
3. 进入《我的世界》手游/PC版
4. 手持「空白乐谱」右键「八音盒」
5. 点击「导入乐谱」按钮
6. 空白乐谱变为「自定义乐谱」，点击「开始播放」

## UI/UX 设计要求

### 整体风格
- 柔和音乐主题：暖色调（棕色、米色、金色系）
- 圆角卡片式设计
- 字体使用无衬线字体（如 PingFang SC / Microsoft YaHei）

### 布局（PC端）
```
┌──────────────────────────────────────────────────────┐
│  🎵 MIDI 转八音盒乐谱工具               [使用说明]    │
├──────────────────────────────────────────────────────┤
│ ┌─ 上传 MIDI 文件 ───────────────────────────────┐   │
│ │  [ 📂 选择文件 ] 或拖拽 MIDI 文件到此处          │   │
│ │  已选择: 天空之城.mid (12.5 KB)                  │   │
│ └────────────────────────────────────────────────┘   │
│                                                      │
│ ┌─ 转换结果 ──────────────────────────────────────┐   │
│ │  曲名: 天空之城  BPM: 120  音符: 128 个          │   │
│ │  时长: 32.0 秒  跳过黑键: 15  范围外: 0          │   │
│ │  前 10 个音符:                                   │   │
│ │  C5(idx=7) @tick=0 dur=1 vol=1.0                │   │
│ │  E5(idx=9) @tick=0 dur=1 vol=1.0                │   │
│ │  ...                                             │   │
│ └────────────────────────────────────────────────┘   │
│                                                      │
│  [🔄 转换]  [📋 复制 JSON]  [💾 下载文件]           │
│                                                      │
│ ┌─ 乐谱 JSON 预览 ─────────────────────────────────┐  │
│ │  {                                                  │
│ │    "v": 1,                                          │
│ │    "n": "天空之城",                                 │
│ │    "b": 120,                                        │
│ │    "t": [[7,0,1,1.0], [9,0,1,1.0], ...]            │
│ │  }                                                  │
│ └──────────────────────────────────────────────────┘  │
│                                                      │
│ ┌─ 手动导入乐谱 ──────────────────────────────────┐   │
│ │  [在此粘贴乐谱 JSON 数据...]                       │   │
│ │  [📖 解析预览]                                     │   │
│ └────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

### 响应式（手机端）
- 宽度 ≤ 768px 时单列布局
- 按钮全宽显示
- 字体和控件大小适配触屏（最小触摸目标 44px）
- JSON 预览区域可横向滚动

## 技术实现要求

### MIDI 解析
- 使用 JavaScript 库 parse-midi 或 midi-parser-js（通过 CDN 引入）
- 纯浏览器端解析，不上传文件到服务器

### 文件格式
- 输入：Standard MIDI File (.mid/.midi)
- 输出：JSON（符合上述乐谱格式）

### 兼容性
- 桌面端：Chrome、Edge、Firefox
- 移动端：手机自带浏览器、微信内置浏览器

## 成功标准
1. 用户上传 MIDI 后能正确转换
2. 复制 JSON 后能在游戏内成功导入八音盒
3. 手机端操作流畅，布局自适应
4. 页面加载速度快（纯静态页面，无后端依赖）
